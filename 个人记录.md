# 软件服务管理平台——阶段学习与项目技术总结

> 更新时间：2026-09-09
> 项目：SoftwareServicePlatform
> 技术方向：React + TypeScript + ASP.NET Core Web API + EF Core + PostgreSQL
> 目的：用于个人复习、项目复盘、面试准备和简历整理。

---

# 一、30 秒快速回顾

这个项目解决的核心问题是：

```text
公司软件通过微信等方式分发
↓
客户版本不统一
↓
问题直接找开发
↓
没有统一的问题处理记录
↓
Bug、版本、客户升级之间无法追踪
```

因此设计了一套软件服务管理平台：

```text
客户
 ↓
软件
 ↓
软件版本
 ↓
客户软件授权
 ↓
安装包发布 / 下载
 ↓
客户问题
 ↓
Ticket 工单
 ↓
开发处理
 ↓
Bug
 ↓
修复版本
 ↓
客户升级
```

目前已经实际开发或学习到的核心模块：

```text
React + TypeScript 前端             ✅

ASP.NET Core Web API               ✅

PostgreSQL + EF Core               ✅

客户管理 CRUD                       ✅

软件管理                            ✅

版本管理                            ✅

安装包上传 / 下载                   ✅

客户软件授权                        ✅

JWT 登录认证                        ✅

RBAC 角色权限                       ✅

用户管理                            ✅

客户门户                            ✅

平台名称 / 公司名称配置             ✅

Ticket 工单创建                     ✅

Ticket 权限查询                     ✅

Ticket 工单分配                     ✅

工单处理记录                        下一阶段
Bug 管理                            下一阶段
```

---

# 二、整个项目的技术架构

目前系统可以理解为：

```text
Browser
   ↓
React + TypeScript
   ↓
fetch / apiFetch
   ↓
Vite Proxy
   ↓
ASP.NET Core Web API
   ↓
Controller
   ↓
Entity Framework Core
   ↓
Npgsql
   ↓
PostgreSQL
```

开发环境：

```text
前端：
React
TypeScript
Vite
React Router
HTML / CSS

后端：
.NET 10
ASP.NET Core Web API
EF Core 10
JWT
PasswordHasher
MemoryCache

数据库：
PostgreSQL

开发工具：
Visual Studio 2022
VS Code
DBeaver
Git
GitHub
```

---

# 三、我对 Web 全栈开发最大的理解

以前主要做：

```text
C++ / Qt
C# / WinForms
```

桌面程序通常：

```text
按钮点击
↓
业务代码
↓
数据库
↓
界面刷新
```

Web 项目则变成：

```text
React UI
↓
HTTP Request
↓
Controller
↓
业务逻辑
↓
数据库
↓
JSON Response
↓
React State
↓
重新渲染
```

核心变化就是：

> 前端和后端不再直接调用函数，而是通过 HTTP API 通信。

例如桌面软件可能：

```csharp
LoadCustomers();
```

Web 中则是：

```tsx
const response = await fetch("/api/customers");

const data = await response.json();
```

对应后端：

```csharp
[HttpGet]
public async Task<IActionResult> GetCustomers()
{
    var customers =
        await _dbContext.Customers
            .AsNoTracking()
            .ToListAsync();

    return Ok(customers);
}
```

---

# 四、React 中学到的核心知识

## 1. Component 组件

React 页面实际上就是：

```tsx
function CustomerPage() {
    return (
        <div>
            客户管理
        </div>
    );
}
```

可以理解成 Qt / WinForms 中：

```text
一个 UserControl
一个 Widget
一个 Form
```

但 React Component 更强调：

```text
数据
↓
决定界面
```

---

# 五、useState —— React 中最重要的状态概念

例如：

```tsx
const [username, setUsername] = useState("");
```

可以理解为：

```text
username
=
当前界面上的用户名数据

setUsername
=
修改 username 的方法
```

例如输入框：

```tsx
<input
    value={username}
    onChange={(e) => setUsername(e.target.value)}
/>
```

流程：

```text
用户输入
↓
onChange
↓
setUsername
↓
username 改变
↓
React 自动刷新相关 UI
```

和 WinForms 最大区别：

WinForms 常写：

```csharp
textBox1.Text = "abc";
```

React 更强调：

```text
State 改变
↓
UI 自动跟着改变
```

---

# 六、useEffect —— 页面加载逻辑

例如平台名称：

```tsx
useEffect(() => {

    async function loadPlatformInfo() {

        const info =
            await getPlatformInfo();

        setPlatformTitle(info.title);
    }

    loadPlatformInfo();

}, []);
```

其中：

```tsx
[]
```

表示：

> Component 第一次加载时执行一次。

可以类比：

```csharp
Form_Load
```

或 Qt：

```cpp
构造完成后的初始化逻辑
```

---

# 七、TypeScript Interface

例如：

```tsx
interface UserItem {
    id: number;
    username: string;
    displayName: string;
    role: string;
    customerId: number | null;
}
```

TypeScript 帮助我们明确 API 返回结构。

类似 C#：

```csharp
public class UserItem
{
    public int Id { get; set; }

    public string Username { get; set; }

    public string Role { get; set; }
}
```

这样就不是 JavaScript 那种完全动态类型。

---

# 八、TypeScript 类型断言

实际使用过：

```tsx
const data = await response.json() as UserItem[];
```

需要记住：

```tsx
as UserItem[]
```

表示：

> 告诉 TypeScript，我认为这个数据应该是 `UserItem[]`。

特别注意：

```tsx
const data = await response.json() as UserItem[];
```

保持这种写法，不要把 `as Xxx` 随意拆行。

---

# 九、React 条件渲染

实际用户管理中：

```tsx
{newRole === "Customer" && (
    <div className="form-item">
        ...
    </div>
)}
```

意思：

```text
如果：

newRole == Customer

才显示：

所属客户
```

类似 C#：

```csharp
if (newRole == "Customer")
{
    customerPanel.Visible = true;
}
```

React 是通过 JSX 条件决定是否创建 UI。

---

# 十、React Router

项目已经使用：

```text
React Router
```

路由例如：

```tsx
<Route path="/customers" element={<CustomerPage />} />

<Route path="/software" element={<SoftwarePage />} />

<Route path="/users" element={<UserPage />} />
```

意味着：

```text
/customers
↓
CustomerPage

/software
↓
SoftwarePage
```

不需要传统网站那样每个页面一个 HTML。

---

# 十一、Outlet 和后台布局

后台结构：

```text
MainLayout
├─ Header
├─ Sidebar
└─ Outlet
```

其中：

```tsx
<Outlet />
```

可以理解为：

> 当前路由页面要显示的位置。

例如访问：

```text
/customers
```

最终：

```text
MainLayout
├─ Header
├─ Sidebar
└─ CustomerPage
```

---

# 十二、前端登录保护 RequireAuth

目前已经实现：

```tsx
<Route element={<RequireAuth />}>
```

所有后台页面首先检查：

```text
是否已经登录
```

如果没有：

```text
→ /login
```

这解决的是：

> UI 页面是否允许进入。

---

# 十三、RBAC 角色权限

目前角色：

```text
Admin
Support
Developer
Sales
Customer
```

中文：

```text
Admin       系统管理员

Support     售后人员

Developer   开发人员

Sales       销售人员

Customer    客户用户
```

页面可以这样保护：

```tsx
<RequireRole
    allowedRoles={[
        "Admin",
        "Support"
    ]}
/>
```

表示只有：

```text
Admin
Support
```

能进入。

---

# 十四、必须记住：前端权限不等于安全

前端：

```tsx
RequireRole
```

主要作用：

```text
菜单隐藏
页面跳转
用户体验
```

真正的安全必须在后端：

```csharp
[Authorize(Roles = "Admin")]
```

即使有人：

```text
打开浏览器开发者工具
修改前端代码
自己构造 HTTP 请求
```

后端依然会拒绝。

这是 Web 开发非常重要的安全原则：

> 永远不要相信前端。

---

# 十五、ASP.NET Core Controller

Controller 是前端和后端业务之间的入口。

例如：

```csharp
[ApiController]

[Route("api/[controller]")]

public class UsersController
    : ControllerBase
{
}
```

那么：

```text
UsersController
```

默认对应：

```text
/api/users
```

---

# 十六、HTTP 方法

目前实际使用过：

```text
GET

查询
```

例如：

```http
GET /api/users
```

---

```text
POST

创建 / 执行动作
```

例如：

```http
POST /api/tickets
```

---

```text
PUT

修改
```

例如：

```http
PUT /api/users/5
```

---

```text
DELETE

删除
```

例如：

```http
DELETE /api/customers/5
```

---

# 十七、DTO

后端没有直接让前端提交数据库 Entity，而是逐步使用：

```text
DTO
```

例如：

```csharp
public class CreateTicketRequest
{
    public int SoftwareId { get; set; }

    public string Title { get; set; }

    public string Description { get; set; }

    public string Priority { get; set; }
}
```

非常重要的是这里故意没有：

```text
CustomerId

CreatedByUserId
```

因为不能相信前端提供身份。

---

# 十八、DTO 和 Entity 的区别

Entity：

```text
代表数据库表
```

例如：

```csharp
Ticket
User
Customer
Software
```

DTO：

```text
代表 API 输入 / 输出数据
```

例如：

```csharp
CreateTicketRequest
AssignTicketRequest
LoginRequest
```

这是很重要的后端设计习惯。

---

# 十九、Entity Framework Core

EF Core 可以理解为：

```text
C# Object
↕
Database Table
```

例如：

```csharp
public DbSet<User> Users { get; set; }
```

对应：

```text
Users 表
```

查询：

```csharp
var user =
    await _dbContext.Users
        .FirstOrDefaultAsync(
            x => x.Id == id
        );
```

类似 SQL：

```sql
SELECT *
FROM Users
WHERE Id = ...
```

---

# 二十、AsNoTracking

查询列表时使用：

```csharp
.AsNoTracking()
```

例如：

```csharp
var users =
    await _dbContext.Users
        .AsNoTracking()
        .ToListAsync();
```

意思：

> 这些数据只是查询，不准备修改。

EF 不需要跟踪实体状态。

优点：

```text
减少内存
减少跟踪开销
查询更轻
```

---

# 二十一、什么时候不能 AsNoTracking

如果之后要修改：

```csharp
var ticket =
    await _dbContext.Tickets
        .FirstOrDefaultAsync(
            x => x.Id == id
        );

ticket.Status =
    "Processing";

await _dbContext.SaveChangesAsync();
```

这里不能随便：

```csharp
AsNoTracking()
```

因为我们需要 EF 跟踪 Ticket 的变化。

---

# 二十二、IQueryable

Ticket 查询中开始使用：

```csharp
IQueryable<Ticket> query =
    _dbContext.Tickets
        .AsNoTracking();
```

然后根据角色动态增加：

```csharp
query =
    query.Where(
        x =>
            x.CustomerId
            ==
            currentUser.CustomerId
    );
```

最终：

```csharp
var tickets =
    await query
        .OrderByDescending(
            x => x.CreatedAt
        )
        .ToListAsync();
```

关键理解：

```text
IQueryable
↓
先拼查询条件
↓
Where
↓
OrderBy
↓
Select
↓
ToListAsync
↓
真正执行 SQL
```

以后做：

```text
搜索
过滤
排序
分页
```

都离不开这个模式。

---

# 二十三、EF Core Navigation Property

例如 Ticket：

```csharp
public int CustomerId { get; set; }

public Customer Customer { get; set; }
```

这两个分别叫：

```text
Foreign Key
Navigation Property
```

通过：

```csharp
x.Customer.Name
```

可以直接访问关联客户。

---

# 二十四、一对多关系

当前系统已经出现很多一对多：

```text
Software
1
↓
N
SoftwareVersion
```

```text
Customer
1
↓
N
User
```

```text
Customer
1
↓
N
Ticket
```

```text
Software
1
↓
N
Ticket
```

---

# 二十五、多对多关系

客户和软件是：

```text
Customer
N
↕
N
Software
```

通过：

```text
CustomerSoftware
```

中间表实现：

```text
Customer
    ↓
CustomerSoftware
    ↓
Software
```

字段：

```text
CustomerId
SoftwareId
IsEnabled
BoundAt
```

并增加联合唯一约束：

```csharp
modelBuilder
    .Entity<CustomerSoftware>()
    .HasIndex(
        x => new
        {
            x.CustomerId,
            x.SoftwareId
        }
    )
    .IsUnique();
```

避免：

```text
同一客户
重复绑定
同一软件
```

---

# 二十六、DeleteBehavior

学习了：

```text
Cascade
Restrict
SetNull
```

例如：

```csharp
.OnDelete(
    DeleteBehavior.Cascade
);
```

意思：

```text
主记录删除
↓
子记录一起删除
```

适合：

```text
Software
→ SoftwareVersion
```

某些场景。

---

历史业务记录则更适合：

```csharp
DeleteBehavior.Restrict
```

例如 Ticket：

```text
Customer
↓
Ticket
```

不能因为客户被删除，就把全部历史工单一起删除。

---

```csharp
DeleteBehavior.SetNull
```

适用于：

```text
用户被删除
↓
AssignedToUserId = null
```

---

# 二十七、Migration

修改：

```text
Model
DbContext
Relationship
```

以后需要：

```bash
dotnet ef migrations add AddTicket
```

然后：

```bash
dotnet ef database update
```

理解：

```text
Model 修改
↓
Migration
↓
数据库结构修改
```

Migration 类似：

> 数据库结构的 Git 历史。

---

# 二十八、PostgreSQL

目前已经真正使用：

```text
PostgreSQL
```

通过：

```text
Npgsql
```

连接 ASP.NET Core。

连接字符串：

```text
Host
Port
Database
Username
Password
```

敏感连接信息放到：

```text
appsettings.Development.json
```

并避免提交到 Git。

---

# 二十九、密码不能明文保存

User 中不是：

```csharp
Password
```

而是：

```csharp
PasswordHash
```

创建密码：

```csharp
user.PasswordHash =
    _passwordHasher.HashPassword(
        user,
        request.Password
    );
```

登录验证：

```csharp
_passwordHasher
    .VerifyHashedPassword(
        user,
        user.PasswordHash,
        request.Password
    );
```

核心原则：

> 数据库永远不要保存用户明文密码。

管理员也不能看到原密码。

只能：

```text
Reset Password
```

---

# 三十、JWT Authentication

用户登录：

```text
username
password
↓
验证数据库
↓
生成 JWT
↓
前端保存
```

JWT 中目前包含：

```text
UserId

Username

Role

DisplayName

CustomerId
```

例如：

```csharp
new Claim(
    ClaimTypes.NameIdentifier,
    user.Id.ToString()
);
```

后端之后可以：

```csharp
var userId =
    User.FindFirstValue(
        ClaimTypes.NameIdentifier
    );
```

---

# 三十一、JWT 最大的理解

JWT 类似：

> 服务器签名的用户身份证。

登录一次：

```text
服务器
↓
签发 Token
```

之后请求：

```http
Authorization: Bearer eyJ...
```

后端就知道：

```text
你是谁
你是什么角色
你属于哪个客户
```

---

# 三十二、JWT 是无状态的

目前系统有一个值得记住的知识点：

```text
用户登录
↓
拿到 JWT
↓
管理员后来停用用户
↓
旧 JWT 在过期前理论上仍可能存在
```

因此对于重要操作，项目中已经开始增加：

```csharp
var currentUser =
    await _dbContext.Users
        .FirstOrDefaultAsync(
            x => x.Id == currentUserId
        );

if (!currentUser.IsEnabled)
{
    return Unauthorized();
}
```

即：

```text
JWT 身份
+
数据库当前状态
```

双重检查。

以后可以继续学习：

```text
Refresh Token
Token Version
Security Stamp
Token Revocation
```

---

# 三十三、apiFetch

前端已经建立统一请求方法。

核心作用：

```text
普通 fetch
+
自动携带 JWT
+
统一处理 401
```

思想类似：

```tsx
fetch(url, {
    headers: {
        Authorization:
            `Bearer ${token}`
    }
});
```

这样每个页面不用重复写认证逻辑。

---

# 三十四、sessionStorage

当前保存：

```text
access_token

current_user
```

例如：

```tsx
sessionStorage.setItem(
    "access_token",
    loginResult.token
);
```

特点：

```text
当前浏览器标签会话使用
关闭会话后清理
```

以后还可以继续研究：

```text
HttpOnly Cookie
localStorage
sessionStorage
```

之间的安全区别。

---

# 三十五、客户数据隔离

这是整个系统非常重要的后端安全思想。

错误做法：

```http
GET /api/customer/software?customerId=3
```

客户可以自己改：

```text
3
↓
5
```

查看别人的数据。

正确做法：

```text
JWT
↓
CustomerId
↓
后端自己确定客户
```

例如：

```csharp
var customerIdText =
    User.FindFirstValue(
        "customerId"
    );
```

客户不能自己指定身份。

---

# 三十六、客户软件权限检查

创建 Ticket 时：

前端只传：

```json
{
  "softwareId": 3
}
```

但后端还必须确认：

```csharp
var hasSoftware =
    await _dbContext
        .CustomerSoftwares
        .AnyAsync(
            x =>
                x.CustomerId == customerId
                &&
                x.SoftwareId ==
                    request.SoftwareId
                &&
                x.IsEnabled
        );
```

防止：

```text
客户通过修改 HTTP Request
给自己没有购买的软件提交工单
```

这是典型：

> 前端限制只是体验，后端校验才是权限。

---

# 三十七、用户管理

已经实现：

```text
用户列表

搜索用户

新增内部用户

编辑用户

修改角色

启用 / 停用

客户用户绑定 Customer

重新绑定 Customer

管理员重置密码
```

其中内部人员：

```text
CustomerId = null
```

客户账号：

```text
Role = Customer

CustomerId = 某客户
```

这是一个比较合理的统一用户模型。

---

# 三十八、自我保护逻辑

用户管理中学习了一个很好的业务规则：

当前 Admin：

```text
不能停用自己
```

也不能：

```text
把自己从 Admin
改成普通角色
```

原因：

否则可能把系统最后一个管理员权限锁死。

这属于：

```text
业务安全
```

而不仅仅是技术安全。

---

# 三十九、平台品牌配置

为了以后给不同公司部署：

没有把：

```text
软件服务管理平台
```

全部写死在 React。

后端配置：

```json
"Platform": {
    "Title":
        "软件服务管理平台",

    "CompanyName":
        "XX科技有限公司"
}
```

提供：

```http
GET /api/platform/info
```

前端：

```tsx
const info =
    await getPlatformInfo();

setPlatformTitle(
    info.title
);
```

这样以后部署给不同公司：

```text
不改 React
不重新改代码
```

只需要修改配置。

这属于非常典型的：

```text
配置化设计
```

---

# 四十、安装包上传与下载

项目已经实现：

```text
软件
↓
版本
↓
安装包
```

并设计客户下载权限。

为了避免直接暴露：

```text
服务器真实文件路径
```

使用短期 Download Ticket：

```text
客户请求下载
↓
后端验证权限
↓
生成短期 Ticket
↓
返回 Ticket
↓
客户下载
↓
后端再次验证
```

接口类似：

```text
POST
/api/download/version/{versionId}/ticket
```

然后：

```text
GET
/api/download/file?ticket=xxx
```

---

# 四十一、为什么下载使用 MemoryCache

短期 Ticket 不一定需要永久存数据库。

使用：

```text
MemoryCache
```

保存：

```text
Ticket
User
Version
过期时间
```

例如：

```text
5 分钟有效
```

这让我学到了：

> 不是所有数据都必须写数据库。

临时数据可以使用 Cache。

---

# 四十二、HTTP Range

软件下载已经考虑：

```text
Range Request
```

主要用于：

```text
大文件
断点续传
浏览器重试
```

因此 Download Ticket 不能：

```text
第一次请求以后马上删除
```

因为浏览器可能进行多次 Range Request。

这是一个比较实际的 Web 文件下载知识点。

---

# 四十三、Ticket 工单模型

当前设计：

```text
Ticket
```

主要字段：

```text
Id

TicketNo

Title

Description

Status

Priority

CustomerId

SoftwareId

CreatedByUserId

AssignedToUserId

CreatedAt

UpdatedAt

ResolvedAt
```

关系：

```text
Customer
    ↓
Ticket

Software
    ↓
Ticket

User
    ↓
CreatedByUser

User
    ↓
AssignedToUser
```

---

# 四十四、Ticket 和 Bug 不是一回事

这是当前项目一个很重要的业务设计。

客户反馈：

```text
不会使用软件

安装问题

配置问题

数据异常

功能咨询

软件缺陷
```

全部首先应该是：

```text
Ticket
```

只有确认是程序问题：

```text
Ticket
↓
Bug
```

因此：

```text
Ticket ≠ Bug
```

这个设计比：

> 客户反馈一个问题就直接创建 Bug

更加合理。

---

# 四十五、Ticket 状态设计

当前：

```text
Pending
```

代表：

```text
客户已经提交
暂时没人正式处理
```

---

```text
Processing
```

代表：

```text
已经分配处理人员
正在处理
```

---

后续：

```text
Resolved
```

代表：

```text
问题已经有解决结果
```

---

```text
Closed
```

代表：

```text
工单正式结束
```

---

# 四十六、Ticket 优先级

当前：

```text
Low

Normal

High

Urgent
```

以后可以用于：

```text
排序
筛选
SLA
统计
```

---

# 四十七、工单号生成

工单不直接使用：

```text
Id = 18
```

给客户看。

而生成：

```text
TK20260909000018
```

例如：

```csharp
ticket.TicketNo =
    $"TK{now:yyyyMMdd}{ticket.Id:D6}";
```

其中：

```text
TK
+
日期
+
6位 ID
```

有利于：

```text
客服沟通
电话沟通
微信沟通
日志查询
```

---

# 四十八、工单创建安全设计

Customer 创建 Ticket 时：

前端只需要传：

```json
{
    "softwareId": 1,

    "title":
        "软件启动失败",

    "description":
        "启动后报错",

    "priority":
        "High"
}
```

后端自动确定：

```text
CustomerId

CreatedByUserId

Status

CreatedAt
```

不能让前端随便传。

---

# 四十九、工单角色查询范围

目前规则：

```text
Admin
→ 全部

Support
→ 全部

Developer
→ 分配给自己的

Customer
→ 自己公司的

Sales
→ 暂无权限
```

这是典型：

```text
RBAC
+
数据权限
```

需要注意：

角色权限和数据权限不是同一个概念。

例如 Customer：

```text
有 GET Ticket 权限
```

但是：

```text
只能 GET 本公司 Ticket
```

这叫：

```text
Row Level Data Permission
```

虽然目前没有使用数据库原生 RLS，而是在业务层实现。

---

# 五十、工单分配

Admin / Support：

```http
PUT /api/tickets/{id}/assign
```

请求：

```json
{
    "assignedToUserId": 8
}
```

后端：

```csharp
ticket.AssignedToUserId =
    assignedUser.Id;

ticket.Status =
    "Processing";

ticket.UpdatedAt =
    DateTime.UtcNow;
```

关键思想：

> 前端只表达“我要执行什么操作”，业务状态由后端决定。

不要让前端直接传：

```json
{
    "status":
        "Processing"
}
```

随便控制业务状态。

---

# 五十一、业务接口应该表达动作

例如：

```text
PUT /api/tickets/{id}/assign
```

比简单：

```text
PUT /api/tickets/{id}
```

然后随便传所有字段更清楚。

以后还可以：

```text
/tickets/{id}/resolve

/tickets/{id}/close

/tickets/{id}/reopen
```

这叫：

```text
业务动作 API
```

可以避免业务状态被任意修改。

---

# 五十二、API 返回不要直接返回 Entity

不建议：

```csharp
return Ok(user);
```

因为可能包含：

```text
PasswordHash
Navigation Property
内部字段
```

现在更多使用：

```csharp
return Ok(new
{
    user.Id,
    user.Username,
    user.DisplayName
});
```

或者后续使用：

```text
Response DTO
```

这是很重要的安全习惯。

---

# 五十三、状态码

目前实际使用：

```text
200 OK

请求成功
```

```text
201 Created

创建成功
```

```text
400 Bad Request

参数错误 / 业务校验失败
```

```text
401 Unauthorized

未登录 / Token 无效 / 身份失效
```

```text
403 Forbidden

已经登录，但没有权限
```

```text
404 Not Found

目标不存在
```

需要明确：

```text
401
≠
403
```

401：

```text
你是谁？
我无法认可你的身份。
```

403：

```text
我知道你是谁，
但是你没有权限。
```

---

# 五十四、Vite Proxy

前端请求：

```tsx
fetch("/api/users")
```

实际上由 Vite：

```text
/api
↓
http://localhost:5108
```

所以开发代码不用：

```tsx
fetch(
    "http://localhost:5108/api/users"
);
```

优点：

```text
减少地址硬编码

开发方便

未来部署更容易
```

---

# 五十五、平台配置接口为什么 AllowAnonymous

登录页面也需要平台名称。

但登录页面：

```text
还没有 JWT
```

所以：

```csharp
[AllowAnonymous]

[HttpGet("info")]
```

允许：

```text
未登录用户
```

获取：

```text
Platform Title
CompanyName
```

这种数据本来就不是敏感数据。

---

# 五十六、前端公共组件思想

已经逐步抽取：

```text
PageHeader

SearchBar

StatusBadge

ConfirmDeleteButton

RequireAuth

RequireRole
```

目的：

```text
重复 UI
↓
公共 Component
```

例如多个页面都用搜索：

```tsx
<SearchBar
    value={keyword}
    placeholder="搜索用户"
    onChange={setKeyword}
    onClear={() =>
        setKeyword("")
    }
/>
```

以后改搜索框样式，只改一个组件。

---

# 五十七、代码复用和不要过度抽象

当前学习过程中一个很重要的原则：

简单重复：

```text
可以抽公共组件
```

但是不要过早设计：

```text
超级通用 Form Engine
超级通用 Repository
超级通用 CRUD Framework
```

当前项目以：

```text
业务清晰
容易理解
容易调试
```

为第一目标。

---

# 五十八、Git 工作流

现在项目已经开始按：

```text
main
+
feature
+
fix
```

开发。

开始新功能：

```bash
git switch main

git pull

git switch -c feature/ticket-management
```

开发：

```bash
git add .

git commit -m "feat: add customer ticket creation"
```

推送：

```bash
git push -u origin feature/ticket-management
```

然后：

```text
Pull Request
↓
Review
↓
Merge
```

合并：

```bash
git switch main

git pull

git branch -d feature/ticket-management
```

---

# 五十九、Commit 规范

已经接触：

```text
feat

新增功能
```

```text
fix

修复 Bug
```

```text
refactor

重构
```

```text
docs

文档
```

```text
style

样式
```

```text
chore

配置 / 工程
```

例如：

```bash
git commit -m "feat: add ticket assignment"
```

---

# 六十、从桌面开发经验迁移到 Web

已有 C#/C++ 经验其实帮了很多。

可以这样类比：

| Web             | C# / Qt               |
| --------------- | --------------------- |
| React Component | UserControl / QWidget |
| useState        | 成员变量 + UI 状态          |
| useEffect       | Load / 初始化            |
| Controller      | Service / 接口层         |
| DTO             | 请求参数类                 |
| Entity          | 数据模型                  |
| EF Core         | ORM / 数据访问层           |
| Route           | 页面 / API 地址           |
| JWT             | 登录会话身份                |
| Middleware      | 全局处理链                 |
| React Router    | 页面导航                  |
| CSS             | Qt StyleSheet / UI样式  |

---

# 六十一、目前学会的核心安全意识

到现在最值得记住的安全知识包括：

```text
1. 密码不能明文存储

2. JWT 必须签名

3. 后端不能相信前端 Role

4. 后端不能相信前端 UserId

5. 后端不能相信前端 CustomerId

6. 页面隐藏不等于权限控制

7. API 必须再次进行角色校验

8. 客户数据必须隔离

9. 重要操作要重新检查用户状态

10. 返回数据不能包含 PasswordHash

11. 数据库密码不能提交 Git

12. 下载不能直接暴露服务器真实路径
```

---

# 六十二、目前已经形成的完整业务链

现在系统已经可以实现：

```text
管理员
↓
创建客户
↓
创建软件
↓
创建软件版本
↓
上传安装包
↓
把软件授权给客户
↓
创建客户账号
↓
客户登录
↓
查看自己的软件
↓
下载正式安装包
↓
客户提交 Ticket
↓
Support / Admin 查看
↓
分配给 Developer
↓
Developer 查看自己的工单
```

已经开始从单纯 CRUD 进入：

```text
真正业务流程系统
```

阶段。

---

# 六十三、当前仍需继续学习的内容

下一阶段：

```text
TicketRecord
工单处理记录
```

实现：

```text
客户回复

售后回复

开发回复

处理过程时间线
```

然后：

```text
工单解决
工单关闭
工单重新打开
```

之后：

```text
Bug
```

实现：

```text
Ticket
↓
确认程序问题
↓
Bug
↓
Developer
↓
修复 Version
↓
客户升级
```

后续还需要：

```text
附件上传

日志上传

分页

复杂搜索

Dashboard

统计报表

操作日志

通知

Docker

Nginx

生产环境部署

HTTPS

Refresh Token
```

---

# 六十四、面试时如何介绍这个项目

可以这样介绍：

> 我独立开发了一套软件服务管理平台，主要用于解决公司软件版本分发不统一、客户软件授权管理以及售后问题处理流程缺乏追踪的问题。
>
> 项目前端采用 React + TypeScript + Vite，后端使用 ASP.NET Core Web API 和 EF Core，数据库使用 PostgreSQL。
>
> 系统目前已经实现客户管理、软件和版本管理、软件包上传下载、客户软件授权、JWT 登录认证、RBAC 权限控制、用户管理以及客户自助下载等功能，同时正在建设 Ticket 工单系统。
>
> 在权限设计方面，我没有只依赖前端菜单隐藏，而是在 API 层通过 JWT Claims 和 ASP.NET Core Authorize 做角色权限控制，同时对 CustomerId、UserId 等敏感身份信息全部从 JWT 和数据库获取，避免前端伪造数据。
>
> 工单模块支持客户提交问题、售后查看全部工单、管理员或售后分配处理人，以及开发人员查看分配给自己的工单，后续会继续关联 Bug 和修复版本，实现完整的问题闭环。

---

# 六十五、简历可以直接写的项目描述

## 软件服务管理平台

**技术栈：**

```text
React
TypeScript
Vite
React Router
ASP.NET Core Web API
.NET 10
Entity Framework Core
PostgreSQL
JWT
Git
```

项目描述：

> 面向公司软件交付与售后场景设计并开发软件服务管理平台，统一管理客户、软件产品、版本发布、安装包、客户软件授权以及售后工单，解决传统微信分发导致的软件版本混乱和问题处理过程不可追踪问题。

主要工作：

* 基于 React + TypeScript + Vite 搭建前端管理平台，使用 React Router 实现后台页面路由、登录保护及角色页面权限控制。
* 基于 ASP.NET Core Web API + EF Core + PostgreSQL 设计客户、软件、版本、用户、客户软件绑定及 Ticket 等核心业务模型。
* 实现 JWT 登录认证及 Admin、Support、Developer、Sales、Customer 多角色 RBAC 权限体系，并在后端 API 层进行权限校验和客户数据隔离。
* 使用 ASP.NET Core `PasswordHasher` 实现用户密码 Hash 存储、登录验证及管理员密码重置，避免明文密码存储。
* 实现客户与软件多对多授权关系，通过 `CustomerSoftware` 控制客户的软件访问及下载权限。
* 实现软件版本安装包上传、短期 Download Ticket 及 HTTP Range 下载机制，避免直接暴露服务器物理文件路径，并支持大文件下载和断点请求。
* 设计并实现客户门户，客户登录后只能查看和下载所属公司的已授权软件和正式发布版本。
* 设计 Ticket 工单模型，实现客户创建工单、客户数据隔离、角色工单查询及 Admin/Support 向 Support/Developer 分配工单等基础流转能力。
* 将平台标题和公司名称配置化，通过后端公共配置 API 动态加载品牌信息，实现同一套系统面向不同公司部署。
* 使用 Git Feature Branch + Pull Request 工作流管理独立功能开发，逐步建立规范的软件版本管理流程。

---

# 六十六、简历里最有价值的技术关键词

以后投简历可以根据岗位选择：

```text
ASP.NET Core

RESTful API

Entity Framework Core

PostgreSQL

JWT Authentication

RBAC

React

TypeScript

React Router

REST API

LINQ

IQueryable

EF Core Migration

Npgsql

Password Hash

HTTP Range

MemoryCache

Role-based Authorization

Data Isolation

Git Flow

Pull Request
```

---

# 六十七、面试可能会问的问题

## 1. 为什么 JWT 里面已经有 CustomerId，还要查数据库？

回答：

> JWT 是登录时签发的，属于某个时间点的用户状态。如果管理员在 Token 有效期间停用了账号或修改了用户归属，JWT 内的信息不会自动变化，因此在创建工单等重要写操作中，我会根据 Token 中的 UserId 再查询数据库确认用户当前状态。

---

## 2. 为什么客户创建工单不能让前端传 CustomerId？

回答：

> 因为前端请求可以被用户修改。如果直接相信 CustomerId，客户可以伪造其他公司的 CustomerId 创建或查询数据。因此 CustomerId 必须从经过签名验证的 JWT 和数据库用户关系中取得。

---

## 3. 为什么前端 RequireRole 还不够？

回答：

> 前端权限只能控制页面展示，用户仍可以直接使用 HTTP 工具请求 API，因此真正的权限控制必须放在后端，通过 `[Authorize]`、Role Claim 以及业务数据范围检查完成。

---

## 4. 为什么 Ticket 和 Bug 分开？

回答：

> 客户反馈的问题可能是使用问题、配置问题、数据问题或程序缺陷，并不是所有反馈都是 Bug。因此先统一进入 Ticket 流程，确认是软件缺陷后再创建 Bug，并关联具体修复版本，更符合真实售后流程。

---

## 5. 为什么 CustomerSoftware 要建立联合唯一索引？

回答：

> 防止同一 Customer 与同一 Software 重复建立授权记录，在数据库层保证数据一致性，而不是只依靠前端或 Controller 判断。

---

## 6. AsNoTracking 有什么作用？

回答：

> 对只读查询使用 `AsNoTracking` 可以避免 EF Core 为实体建立 Change Tracking，降低不必要的内存和 CPU 开销；需要更新的数据则保持 Tracking。

---

## 7. IQueryable 有什么优势？

回答：

> 可以先动态组合 Where、OrderBy、Select 等条件，直到 ToListAsync 才真正生成并执行 SQL，因此很适合实现权限过滤、搜索、筛选和分页。

---

## 8. 为什么下载用了 Ticket？

回答：

> 如果直接把服务器物理路径或长期下载 URL 暴露给前端，会增加越权和链接泄露风险。我先通过受认证接口验证用户的软件权限并生成短期 Download Ticket，真正下载时再验证 Ticket，实现下载授权和文件路径解耦。

---

# 六十八、项目给我带来的能力提升

从这个项目开始，我不再只是理解：

```text
一个按钮
一个函数
一个数据库查询
```

而开始理解完整系统：

```text
业务需求
↓
数据库设计
↓
实体关系
↓
API 设计
↓
身份认证
↓
权限设计
↓
数据隔离
↓
前端状态
↓
页面路由
↓
业务流程
↓
Git 协作流程
```

这也是从：

```text
桌面软件功能开发
```

向：

```text
完整业务系统开发
```

迈出的重要一步。

---

# 六十九、以后快速复习只看这里

如果时间很少，只需要回顾下面这张图：

```text
React
│
├─ Component
├─ useState
├─ useEffect
├─ Props
├─ TypeScript Interface
├─ React Router
├─ RequireAuth
└─ RequireRole

        ↓ HTTP

ASP.NET Core
│
├─ Controller
├─ DTO
├─ Authorize
├─ JWT Claims
├─ PasswordHasher
└─ Dependency Injection

        ↓

EF Core
│
├─ DbContext
├─ DbSet
├─ Entity
├─ Navigation Property
├─ LINQ
├─ IQueryable
├─ AsNoTracking
├─ Migration
└─ Relationship

        ↓

PostgreSQL
│
├─ PK
├─ FK
├─ Unique Index
└─ Data

业务
│
├─ Customer
├─ Software
├─ Version
├─ CustomerSoftware
├─ User
└─ Ticket

安全
│
├─ Password Hash
├─ JWT
├─ RBAC
├─ 后端权限
├─ Customer 数据隔离
└─ 不相信前端

工程
│
├─ Git
├─ Feature Branch
├─ Commit
├─ Push
├─ Pull Request
└─ Merge
```

---

# 七十、当前阶段最值得记住的 10 个知识点

如果半年后重新看项目，至少记住：

```text
1.
React 的核心是 State 驱动 UI。

2.
前后端通过 HTTP + JSON 通信。

3.
DTO 和数据库 Entity 不应该混为一谈。

4.
EF Core 用 LINQ 操作 PostgreSQL。

5.
IQueryable 可以动态构造查询。

6.
JWT 用来证明当前用户身份。

7.
前端权限不是安全，后端权限才是。

8.
永远不要相信前端传来的 UserId、Role、CustomerId。

9.
业务流程应该由后端控制状态变化。

10.
Git 分支 + PR 是团队开发必须掌握的基本能力。
```

---

# 七十一、下一阶段目标

当前最合理的开发顺序：

```text
TicketRecord
↓
工单处理时间线
↓
工单 Resolve
↓
工单 Close
↓
工单 Reopen

然后：

Bug
↓
Ticket 转 Bug
↓
Bug 分配
↓
关联 SoftwareVersion
↓
修复
↓
Release
↓
客户升级
```

做到这里以后，整个软件服务平台将真正形成：

```text
客户
↓
软件
↓
版本
↓
问题
↓
工单
↓
开发
↓
Bug
↓
修复版本
↓
客户升级
```

完整业务闭环。

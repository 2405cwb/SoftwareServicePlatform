# ============================================================
# 自动更新删除清单
# ============================================================
#
# 新版本已经废弃、需要从旧客户端删除的文件，
# 每行一个相对于软件安装根目录的路径。
#
# 不要写绝对路径，不允许 ../
#
# updater/、version.txt、.update-temp/、.update-backup/
# 属于受保护路径，不允许删除。

# 示例：
# old-plugin.dll
# plugins/LegacyModule.dll
# resources/old-template.json
